// Bookcase Spawning system  version .1
// created December 7, 2000
// designed by Bishop Ebonhand and Zulu

use uo;

program openbook(chr,book)
    var res := SendOpenBook( chr, book );
    if (!res)
        print( "SendOpenBook: " + res );
    endif
endprogram